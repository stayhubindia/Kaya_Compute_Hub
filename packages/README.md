# Shared Packages (`/packages`)

This directory contains shared specifications, schemas, and client SDKs:

- **`contracts/`**: Shared OpenAPI specs, JSON schema definitions, and contract definitions used across web frontend and backend services.
- **`sdk-python/`**: Lightweight Python SDK enabling programmatic submission and status querying of jobs from scripts or remote environments.
